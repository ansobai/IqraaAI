/* eslint-disable */
// Fallback manifest: page text assets are currently missing from this repo.
// Keep this object empty so Metro bundling succeeds and the loader can return null safely.
export const PAGE_ASSET_MODULES: Record<number, number> = {};
